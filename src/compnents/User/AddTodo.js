import React from 'react';
import Card from '../UI/Card';

const AddTodo = props => {
    return(
        <Card>
        <form>
        
        </form>
        </Card>
    );
};

export default AddTodo;
